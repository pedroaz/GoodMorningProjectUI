@page "/"

<div class="question-container">
    @Question.Text
</div>

<div class="answer-container">

    @foreach (var answer in Question.Answers) {
        <button @onclick=" (() => { ClickAnswerButton(answer); } )">@answer.Text</button>
    }
</div>
